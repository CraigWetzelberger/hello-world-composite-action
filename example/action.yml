name: 'Example Action'
description: 'Greet someone'

runs:
  using: "composite"
  steps:
    #- uses: actions/setup-node@v3.6.0
    #  with:
    #    node-version: 16
    #- name: Check Node Version
    #  run: node --version
    

    - name: Set Greeting
      run: pwd
      shell: bash

    - name: Run other.sh
      run: other.sh
      shell: bash

